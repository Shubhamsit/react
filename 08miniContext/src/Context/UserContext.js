import React from 'react'

const UserContext=React.createContext()


export default UserContext;// we will make a provider  and we will wrap all those components which value have to be changed inside UserContext here 
